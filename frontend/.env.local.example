# Frontend env vars — copy to .env.local for local dev

# Postgres on Railway (or local Postgres for dev)
DATABASE_URL=postgres://postgres:dev@localhost:5432/reviewai

# The n8n webhook URL — used by the "Trigger a manual review" button (future)
NEXT_PUBLIC_N8N_WEBHOOK_URL=http://localhost:5678/webhook/pr-review
